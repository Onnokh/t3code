diff --git a/build/Widgets.d.ts b/build/Widgets.d.ts
index 6886a8f..14536f8 100644
--- a/build/Widgets.d.ts
+++ b/build/Widgets.d.ts
@@ -111,6 +111,13 @@ export declare function addUserInteractionListener(listener: ExpoWidgetsEvents['
  * @return An event subscription that can be used to remove the listener.
  */
 export declare function addPushToStartTokenListener(listener: ExpoWidgetsEvents['onExpoWidgetsPushToStartTokenReceived']): EventSubscription;
+/**
+ * Adds a listener for Live Activity push token events, for every activity on
+ * this device including ones started remotely by a push.
+ * @param listener Callback function to handle push token events.
+ * @return An event subscription that can be used to remove the listener.
+ */
+export declare function addActivityTokenListener(listener: (event: PushTokenEvent) => void): EventSubscription;
 /**
  * A directory that can be used to store shared images for widgets.
  * The contents of this directory are accessible by both the main app and widgets.
diff --git a/build/Widgets.js b/build/Widgets.js
index 6cd5127..d53317a 100644
--- a/build/Widgets.js
+++ b/build/Widgets.js
@@ -155,6 +155,16 @@ export function addUserInteractionListener(listener) {
 export function addPushToStartTokenListener(listener) {
     return ExpoWidgetsModule.addListener('onExpoWidgetsPushToStartTokenReceived', listener);
 }
+/**
+ * Adds a listener for Live Activity push token events, for every activity on
+ * this device including ones started remotely by a push. Use it to keep a
+ * server able to update and end a card it started itself.
+ * @param listener Callback function to handle push token events.
+ * @return An event subscription that can be used to remove the listener.
+ */
+export function addActivityTokenListener(listener) {
+    return ExpoWidgetsModule.addListener('onExpoWidgetsActivityTokenReceived', listener);
+}
 /**
  * A directory that can be used to store shared images for widgets.
  * The contents of this directory are accessible by both the main app and widgets.
diff --git a/ios/WidgetsModule.swift b/ios/WidgetsModule.swift
index 678a7d1..87e9925 100644
--- a/ios/WidgetsModule.swift
+++ b/ios/WidgetsModule.swift
@@ -7,15 +7,17 @@ let pushNotificationsEnabledKey: String = "ExpoWidgets_EnablePushNotifications"
 let onUserInteraction = "onExpoWidgetsUserInteraction"
 let onPushToStartTokenReceived = "onExpoWidgetsPushToStartTokenReceived"
 let onTokenReceived = "onExpoWidgetsTokenReceived"
+let onActivityTokenReceived = "onExpoWidgetsActivityTokenReceived"
 let onUserInteractionNotification = Notification.Name(onUserInteraction)
 
 public final class WidgetsModule: Module {
   var pushToStartTokenObserverTask: Task<Void, Never>?
+  var activityTokenObserverTask: Task<Void, Never>?
 
   public func definition() -> ModuleDefinition {
     Name("ExpoWidgets")
 
-    Events(onPushToStartTokenReceived, onTokenReceived, onUserInteraction)
+    Events(onPushToStartTokenReceived, onTokenReceived, onActivityTokenReceived, onUserInteraction)
 
     OnStartObserving(onUserInteraction) {
       NotificationCenter.default.addObserver(
@@ -45,6 +47,17 @@ public final class WidgetsModule: Module {
       pushToStartTokenObserverTask = nil
     }
 
+    OnStartObserving(onActivityTokenReceived) {
+      if pushNotificationsEnabled {
+        observeActivityTokens()
+      }
+    }
+
+    OnStopObserving(onActivityTokenReceived) {
+      activityTokenObserverTask?.cancel()
+      activityTokenObserverTask = nil
+    }
+
     Constant("widgetsDirectory") { () -> String? in
       guard let appGroupIdentifier = WidgetsStorage.appGroupIdentifier,
             let containerUrl = FileManager.default.containerURL(forSecurityApplicationGroupIdentifier: appGroupIdentifier) else {
@@ -126,6 +139,53 @@ public final class WidgetsModule: Module {
     )
   }
 
+  private func sendActivityToken(activityId: String, pushToken: String) {
+    sendEvent(
+      onActivityTokenReceived,
+      [
+        "activityId": activityId,
+        "pushToken": pushToken
+      ]
+    )
+  }
+
+  /// Reports the push token of every Live Activity on this device, including
+  /// ones the system started from a push while the app was not running. The
+  /// per-instance listener cannot cover that case: it needs a JS object for an
+  /// activity JS never created, so a remotely started card would stay
+  /// unreachable and could never be updated or ended.
+  @available(iOS 16.1, *)
+  private func observeTokenUpdates(for activity: Activity<LiveActivityAttributes>) {
+    Task {
+      if let data = activity.pushToken {
+        sendActivityToken(
+          activityId: activity.id,
+          pushToken: data.reduce("") { $0 + String(format: "%02x", $1) }
+        )
+      }
+      for await data in activity.pushTokenUpdates {
+        sendActivityToken(
+          activityId: activity.id,
+          pushToken: data.reduce("") { $0 + String(format: "%02x", $1) }
+        )
+      }
+    }
+  }
+
+  private func observeActivityTokens() {
+    guard #available(iOS 16.1, *) else { return }
+    activityTokenObserverTask = Task {
+      // Already-live cards first: the system may have launched the app
+      // precisely because one just started.
+      for activity in Activity<LiveActivityAttributes>.activities {
+        observeTokenUpdates(for: activity)
+      }
+      for await activity in Activity<LiveActivityAttributes>.activityUpdates {
+        observeTokenUpdates(for: activity)
+      }
+    }
+  }
+
   private func observePushToStartToken() {
     guard #available(iOS 17.2, *), ActivityAuthorizationInfo().areActivitiesEnabled else { return }
     pushToStartTokenObserverTask = Task {
diff --git a/src/Widgets.ts b/src/Widgets.ts
index f5bbbba..0233625 100644
--- a/src/Widgets.ts
+++ b/src/Widgets.ts
@@ -226,6 +226,19 @@ export function addPushToStartTokenListener(
   return ExpoWidgetsModule.addListener('onExpoWidgetsPushToStartTokenReceived', listener);
 }
 
+/**
+ * Adds a listener for Live Activity push token events, for every activity on
+ * this device including ones started remotely by a push. Use it to keep a
+ * server able to update and end a card it started itself.
+ * @param listener Callback function to handle push token events.
+ * @return An event subscription that can be used to remove the listener.
+ */
+export function addActivityTokenListener(
+  listener: (event: PushTokenEvent) => void
+): EventSubscription {
+  return ExpoWidgetsModule.addListener('onExpoWidgetsActivityTokenReceived', listener);
+}
+
 /**
  * A directory that can be used to store shared images for widgets.
  * The contents of this directory are accessible by both the main app and widgets.
